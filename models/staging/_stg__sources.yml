version: 2

sources:
  - name: raw
    description: "Bronze layer — source-system tables"
    database: project-b8fc8724-8adc-4499-9a4
    schema: raw

    tables:
      - name: raw_cities
        columns:
          - name: city_id
            data_tests: [not_null, unique]

      - name: raw_drivers
        columns:
          - name: driver_id
            data_tests: [not_null, unique]
          - name: home_city_id
            data_tests: [not_null]

      - name: raw_riders
        columns:
          - name: rider_id
            data_tests: [not_null, unique]

      - name: raw_trips
        columns:
          - name: trip_id
            data_tests: [not_null, unique]
          - name: rider_id
            data_tests: [not_null]
          - name: driver_id
            data_tests: [not_null]
          - name: city_id
            data_tests: [not_null]

      - name: raw_payments
        columns:
          - name: payment_id
            data_tests: [not_null]
          - name: trip_id
            data_tests: [not_null]

      - name: raw_location_pings
        columns:
          - name: ping_id
            data_tests: [not_null]
          - name: driver_id
            data_tests: [not_null]
          - name: ping_timestamp
            data_tests: [not_null]

